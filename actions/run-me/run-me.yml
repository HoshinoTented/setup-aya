name: run-me

inputs:
  aya-root:
    description: the root directory of aya-dev
    required: true

outputs:
  version:
    description: the installed aya version

runs:
  using: node24
  main: dist/index.js
